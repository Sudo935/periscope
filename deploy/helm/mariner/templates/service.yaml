apiVersion: v1
kind: Service
metadata:
  name: {{ include "mariner.fullname" . }}
  labels:
    {{- include "mariner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mariner.selectorLabels" . | nindent 4 }}
